# Repository Metadata Knowledge Base

## `/`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `package.json` | Defines the project configuration and dependency manifest for a React-Redux RealWorld example application. Specifies all runtime and development dependencies including React, Redux, routing libraries, and build tooling. Declares npm scripts for starting the development server on port 4100, building production bundles, running tests, and ejecting from create-react-app configuration. |  |

## `public`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `public/index.html` | Serves as the main HTML template for the Conduit React application. Provides the root DOM mounting point and includes external stylesheets for fonts and UI components from demo.productionready.io and ionicons. |  |

## `src`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/agent.js` | Provides a centralized HTTP client API layer for the Conduit RealWorld application. Wraps superagent to handle authentication token injection and exposes organized service modules (Auth, Articles, Comments, Profile, Tags) for all backend API interactions with the Conduit API. | <b>`setToken: _token => { token = _token; }`</b>: Sets the authentication token that will be automatically injected into all subsequent HTTP requests via the tokenPlugin middleware. |
| `src/index.js` | Serves as the application entry point for a React-Redux single-page application. Bootstraps the React application by rendering the root App component wrapped with Redux Provider and ConnectedRouter for state management and routing, mounting it to the DOM element with id 'root'. |  |
| `src/middleware.js` | Provides Redux middleware for handling asynchronous actions with promise-based payloads and managing JWT authentication tokens in localStorage. Tracks view changes to prevent stale async updates and synchronizes authentication state between the Redux store and the agent service. | <b>`const promiseMiddleware = store => next => action`</b>: Redux middleware that intercepts actions with promise payloads, dispatches async lifecycle events (ASYNC_START/ASYNC_END), and prevents dispatching results if the view has changed during the async operation.<br/><b>`const localStorageMiddleware = store => next => action`</b>: Redux middleware that persists JWT tokens to localStorage on LOGIN/REGISTER actions and clears tokens on LOGOUT, while synchronizing the token state with the agent service.<br/><b>`function isPromise(v)`</b>: Helper function that checks if a value is a promise by verifying the existence of a 'then' method. |
| `src/reducer.js` | Serves as the root Redux reducer configuration for a React-Redux application implementing the RealWorld example app. Combines multiple domain-specific reducers (article, articleList, auth, common, editor, home, profile, settings) and the router reducer into a single unified state tree using Redux's combineReducers utility. | <b>`export default combineReducers({article, articleList, auth, common, editor, home, profile, settings, router: routerReducer})`</b>: Exports the combined root reducer that merges all domain reducers and router state into a single Redux store structure for the application. |
| `src/store.js` | Configures and exports the Redux store for the application, integrating router middleware, custom promise and localStorage middleware, and conditional logging based on environment. Creates and exports the browser history instance for routing integration. | <b>`export const history = createHistory()`</b>: Creates and exports a browser history instance for managing application routing state.<br/><b>`const getMiddleware = () => {...}`</b>: Returns the appropriate middleware configuration based on the environment, including router, promise, localStorage, and optionally logger middleware for non-production environments.<br/><b>`export const store = createStore(reducer, composeWithDevTools(getMiddleware()))`</b>: Creates and exports the Redux store with the root reducer, configured middleware, and Redux DevTools integration for state management. |

## `src/components`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/components/App.js` | Serves as the root React component for a Redux-connected real-world example application. Manages application initialization, authentication token handling, route-based navigation, and conditional rendering of the application shell based on load state. Orchestrates Redux state mapping and dispatching for app-level concerns including redirects and initial data loading. | <b>`componentWillReceiveProps(nextProps)`</b>: Handles navigation redirects by dispatching a push action to the router when redirectTo prop changes and clears the redirect state.<br/><b>`componentWillMount()`</b>: Initializes the application by retrieving the JWT token from localStorage, setting it on the agent, and dispatching the APP_LOAD action with current user data if authenticated.<br/><b>`render()`</b>: Renders the application shell with Header and routing configuration, conditionally displaying full route switch when app is loaded or minimal header during loading. |
| `src/components/ArticleList.js` | Renders a list of article previews with pagination controls for a content feed. Handles loading states and empty states when no articles are available. Maps through article data to display individual ArticlePreview components. | <b>`const ArticleList = props => {...}`</b>: Main functional component that receives articles array, pager, articlesCount, and currentPage as props and conditionally renders loading state, empty state, or a list of article previews with pagination. |
| `src/components/ArticlePreview.js` | Renders a preview card for an article in a React-Redux application, displaying article metadata, author information, and favorite/unfavorite functionality. Connects to Redux store to dispatch favorite and unfavorite actions via the agent API service. Provides navigation links to article detail and author profile pages. | <b>`const mapDispatchToProps = dispatch => ({...})`</b>: Maps Redux dispatch actions to component props, providing favorite and unfavorite functions that dispatch ARTICLE_FAVORITED and ARTICLE_UNFAVORITED actions with API payloads.<br/><b>`const ArticlePreview = props => {...}`</b>: Functional React component that renders an article preview card with author details, creation date, favorite button, title, description, and tag list. Handles click events to toggle article favorite status. |
| `src/components/Editor.js` | Provides a React component for creating and editing articles in a Redux-connected application. Manages form state for article title, description, body, and tags, dispatching Redux actions for field updates, tag management, and article submission. Handles both new article creation and existing article updates based on URL slug parameters. | <b>`constructor()`</b>: Initializes the Editor component, binding event handlers for form field changes, tag management (add/remove), and form submission.<br/><b>`componentWillReceiveProps(nextProps)`</b>: Responds to prop changes, specifically monitoring URL slug changes to load article data for editing or reset the form for new articles.<br/><b>`componentWillMount()`</b>: Loads article data from the API if a slug is present in the URL parameters, or initializes an empty form for new article creation.<br/><b>`componentWillUnmount()`</b>: Dispatches cleanup action to unload editor state when the component is removed from the DOM.<br/><b>`render()`</b>: Renders the article editor form UI with input fields for title, description, body, tags, error display, and a publish button. |
| `src/components/Header.js` | Provides the main navigation header component for a React-Redux application. Conditionally renders navigation links based on user authentication state, displaying different menu options for logged-in versus logged-out users. | <b>`const LoggedOutView = props => {...}`</b>: Renders navigation menu for unauthenticated users with links to Home, Sign in, and Sign up pages.<br/><b>`const LoggedInView = props => {...}`</b>: Renders navigation menu for authenticated users with links to Home, New Post editor, Settings, and user profile page with avatar.<br/><b>`class Header extends React.Component { render() {...} }`</b>: Main header component that renders the application navbar with branding and conditionally displays LoggedInView or LoggedOutView based on currentUser prop. |
| `src/components/ListErrors.js` | Provides a reusable React component that displays validation or API error messages in a formatted list. Accepts an errors object via props and renders each error key-value pair as a list item, returning null if no errors exist. | <b>`render()`</b>: Renders an unordered list of error messages by iterating over the errors object keys, displaying each key and its corresponding error value as a list item. |
| `src/components/ListPagination.js` | Provides a pagination component for article lists in a React-Redux application. Renders clickable page numbers based on article count, dispatching SET_PAGE actions with either a custom pager function or default agent call to fetch paginated article data. | <b>`const mapDispatchToProps = dispatch => ({ onSetPage: (page, payload) => dispatch({ type: SET_PAGE, page, payload }) })`</b>: Maps the onSetPage action dispatcher to component props, enabling pagination state updates through Redux.<br/><b>`const ListPagination = props => { ... }`</b>: Main functional component that renders pagination UI with page numbers, handles page click events, and conditionally uses custom pager or default agent for data fetching. |
| `src/components/Login.js` | Implements the user login page component for a React-Redux application. Manages authentication form state including email and password inputs, dispatches login actions through Redux, and handles form submission with error display. Connects to the Redux store to access authentication state and dispatch authentication-related actions. | <b>`const mapStateToProps = state => ({ ...state.auth })`</b>: Maps the authentication state from the Redux store to the component's props, providing access to email, password, errors, and inProgress status.<br/><b>`const mapDispatchToProps = dispatch => ({ onChangeEmail, onChangePassword, onSubmit, onUnload })`</b>: Maps Redux dispatch functions to component props, enabling email/password field updates, login form submission, and cleanup on component unmount.<br/><b>`constructor()`</b>: Initializes the Login component and binds event handlers for email change, password change, and form submission.<br/><b>`componentWillUnmount()`</b>: Dispatches the LOGIN_PAGE_UNLOADED action to clean up authentication state when the component is unmounted.<br/><b>`render()`</b>: Renders the login form UI with email and password input fields, error display, and a submit button that is disabled during login processing. |
| `src/components/Profile.js` | Implements a user profile page component that displays user information, articles authored by the user, and provides follow/unfollow functionality. Manages profile data loading and unloading through Redux actions and integrates with the article listing system. | <b>`const EditProfileSettings = props => {...}`</b>: Renders a link to the settings page if the current user is viewing their own profile.<br/><b>`const FollowUserButton = props => {...}`</b>: Renders a follow/unfollow button for other users' profiles and handles the follow/unfollow action on click.<br/><b>`const mapStateToProps = state => {...}`</b>: Maps Redux state to component props, providing article list data, current user, and profile information.<br/><b>`const mapDispatchToProps = dispatch => {...}`</b>: Maps Redux dispatch functions to component props for follow, unfollow, load, and unload actions.<br/><b>`componentWillMount()`</b>: Loads profile and articles data when the component is about to mount by dispatching API calls through the agent.<br/><b>`componentWillUnmount()`</b>: Dispatches an unload action to clean up profile state when the component is unmounted.<br/><b>`renderTabs()`</b>: Renders navigation tabs for switching between user's articles and favorited articles.<br/><b>`render()`</b>: Renders the complete profile page layout including user info, follow button, tabs, and article list. |
| `src/components/ProfileFavorites.js` | Renders a user profile page displaying articles favorited by a specific user. Extends the base Profile component to fetch and display favorited articles using Redux state management and React Router for navigation. Provides tab navigation between user's own articles and favorited articles. | <b>`componentWillMount()`</b>: Lifecycle method that dispatches actions to load the user profile and their favorited articles when the component is about to mount.<br/><b>`componentWillUnmount()`</b>: Lifecycle method that dispatches cleanup action to unload profile data when the component is about to unmount.<br/><b>`renderTabs()`</b>: Renders the navigation tabs for switching between user's own articles and favorited articles, with the favorited tab marked as active. |
| `src/components/Register.js` | Implements a user registration form component for a React-Redux application. Manages form state for username, email, and password fields, dispatches registration actions to the Redux store, and handles form submission by calling the authentication agent service. | <b>`const mapStateToProps = state => ({ ...state.auth })`</b>: Maps the authentication state from Redux store to component props, providing access to email, password, username, errors, and inProgress status.<br/><b>`const mapDispatchToProps = dispatch => ({...})`</b>: Maps Redux dispatch functions to component props, enabling form field updates, registration submission, and component cleanup actions.<br/><b>`constructor()`</b>: Initializes the Register component and binds event handlers for email, password, and username field changes, as well as form submission.<br/><b>`componentWillUnmount()`</b>: Dispatches cleanup action to unload the registration page state when the component is unmounted from the DOM.<br/><b>`render()`</b>: Renders the registration form UI with input fields for username, email, and password, displays validation errors, and handles form submission. |
| `src/components/Settings.js` | Provides a user settings management interface for a React-Redux application. Renders a form allowing authenticated users to update their profile information (image, username, bio, email, password) and includes logout functionality. Connects to Redux store to manage state and dispatch actions for saving settings and handling page lifecycle. | <b>`class SettingsForm extends React.Component`</b>: Renders and manages the settings form component with local state for user profile fields. Handles form submission and field updates, syncing with Redux currentUser prop.<br/><b>`componentWillMount()`</b>: Initializes component state with current user data from props when component is about to mount.<br/><b>`componentWillReceiveProps(nextProps)`</b>: Updates component state when new props are received, syncing form fields with updated currentUser data.<br/><b>`render()`</b>: Renders the settings form UI with input fields for image, username, bio, email, and password, along with submit button.<br/><b>`const mapStateToProps = state => ({...state.settings, currentUser: state.common.currentUser})`</b>: Maps Redux state to component props, extracting settings state and currentUser from the store.<br/><b>`const mapDispatchToProps = dispatch => ({onClickLogout, onSubmitForm, onUnload})`</b>: Maps Redux dispatch functions to component props for logout, form submission, and page unload actions.<br/><b>`class Settings extends React.Component`</b>: Main settings page component that renders the page layout, error display, settings form, and logout button. Serves as the connected container component.<br/><b>`export default connect(mapStateToProps, mapDispatchToProps)(Settings)`</b>: Exports the Settings component connected to Redux store with mapped state and dispatch props. |

## `src/components/Article`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/components/Article/ArticleActions.js` | Provides action buttons for article management in a React-Redux application. Renders edit and delete buttons for authorized users, dispatching a DELETE_ARTICLE action when the delete button is clicked and routing to the editor for modifications. | <b>`const mapDispatchToProps = dispatch => ({onClickDelete: payload => dispatch({ type: DELETE_ARTICLE, payload })})`</b>: Maps the delete action dispatcher to component props, enabling the component to dispatch DELETE_ARTICLE actions to the Redux store.<br/><b>`const ArticleActions = props => {...}`</b>: Renders article action buttons (edit and delete) conditionally based on user permissions, handling delete operations through the agent service. |
| `src/components/Article/ArticleMeta.js` | Renders the metadata section for an article display, including author information with avatar and username, article creation date, and action buttons. This component serves as a reusable presentation layer for article header information across the application. | <b>`const ArticleMeta = props => {...}`</b>: Functional React component that accepts article data and canModify flag as props, then renders the complete article metadata UI including author profile link, avatar, username, formatted creation date, and article action controls. |
| `src/components/Article/Comment.js` | Renders a single comment component for an article in a React-based social blogging application. Displays comment metadata including author information, creation date, comment body, and conditionally shows a delete button if the current user is the comment author. | <b>`const Comment = props => {...}`</b>: Functional React component that receives comment data, current user information, and article slug as props, then renders the complete comment UI with author profile links and conditional delete functionality. |
| `src/components/Article/CommentContainer.js` | Renders the comment section container for an article page, conditionally displaying either a comment input form for authenticated users or sign-in/sign-up prompts for unauthenticated visitors. Orchestrates the display of existing comments through the CommentList component in both scenarios. | <b>`const CommentContainer = props => {...}`</b>: Main functional component that conditionally renders comment input and list based on user authentication status, accepting props for currentUser, slug, comments, and errors. |
| `src/components/Article/CommentInput.js` | Provides a React component for submitting comments on an article. Manages local state for the comment body text, handles form submission by calling the agent API to create a comment, and dispatches the ADD_COMMENT action to update the Redux store. | <b>`constructor()`</b>: Initializes component state with an empty body string and binds the setBody and createComment methods for handling user input and form submission.<br/><b>`setBody(ev)`</b>: Updates the component state with the current value of the textarea input as the user types their comment.<br/><b>`createComment(ev)`</b>: Prevents default form submission, calls the agent API to create a comment with the current body text and article slug, clears the input field, and dispatches the result to Redux via onSubmit.<br/><b>`render()`</b>: Renders the comment form UI including a textarea for comment input, the current user's avatar image, and a submit button. |
| `src/components/Article/CommentList.js` | Renders a list of comments for an article by mapping over an array of comment objects and rendering individual Comment components. Serves as a presentational container component that passes down props including the current user, article slug, and comment data to each Comment child component. | <b>`const CommentList = props => {...}`</b>: Functional React component that receives props containing comments array, currentUser, and slug, then maps over comments to render a Comment component for each comment with appropriate props and unique key. |
| `src/components/Article/DeleteButton.js` | Provides a React component that renders a delete button for article comments. When clicked, it dispatches a DELETE_COMMENT action with the comment deletion payload from the agent service, enabling users to remove their own comments from articles. | <b>`const mapDispatchToProps = dispatch => ({onClick: (payload, commentId) => dispatch({ type: DELETE_COMMENT, payload, commentId })})`</b>: Maps the onClick handler to dispatch a DELETE_COMMENT action with the provided payload and commentId to the Redux store.<br/><b>`const DeleteButton = props => {...}`</b>: Renders a trash icon button that triggers comment deletion when clicked, conditionally displayed based on the show prop. |
| `src/components/Article/index.js` | Renders the article detail page for a blog/publishing platform, displaying article content with markdown rendering, author metadata, tags, and an embedded comment section. Connects to Redux store to fetch article and comment data on mount via agent API calls and dispatches lifecycle actions for state management. | <b>`componentWillMount()`</b>: Fetches article details and associated comments from the API using the article ID from route parameters and dispatches the data to Redux store.<br/><b>`componentWillUnmount()`</b>: Dispatches cleanup action to Redux store when component is unmounted to clear article page state.<br/><b>`render()`</b>: Renders the complete article page UI including title, author metadata, markdown-converted article body, tag list, and comment section with conditional edit permissions based on current user. |

## `src/components/Home`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/components/Home/Banner.js` | Renders a promotional banner component for the home page of a React application. Conditionally displays the application name and tagline only when the user is not authenticated (no token present). Serves as a welcoming header for anonymous visitors. | <b>`const Banner = ({ appName, token }) => {}`</b>: Functional React component that accepts appName and token props, returning a banner div with branding when user is not logged in, or null when authenticated. |
| `src/components/Home/MainView.js` | Provides the main view component for the home page, managing tab navigation between 'Your Feed', 'Global Feed', and tag-filtered feeds. Connects to Redux store to dispatch tab change actions and render the appropriate article list based on the selected tab and user authentication state. | <b>`const YourFeedTab = props => {...}`</b>: Renders the 'Your Feed' tab navigation item, visible only when user is authenticated. Dispatches a tab change action to load the user's personalized article feed when clicked.<br/><b>`const GlobalFeedTab = props => {...}`</b>: Renders the 'Global Feed' tab navigation item. Dispatches a tab change action to load all articles from the global feed when clicked.<br/><b>`const TagFilterTab = props => {...}`</b>: Renders a tag filter tab when a specific tag is selected, displaying the tag name with a hash icon to indicate filtered content.<br/><b>`const MainView = props => {...}`</b>: Main container component that renders the feed toggle navigation tabs and the ArticleList component with pagination and loading state management. |
| `src/components/Home/Tags.js` | Renders a list of clickable tag pills for filtering articles by tag in the home page. Displays a loading state when tags are not yet available and triggers article fetching via the agent when a tag is clicked. | <b>`const Tags = props => {...}`</b>: Functional React component that receives tags and onClickTag callback via props, renders each tag as a clickable link that fetches articles filtered by the selected tag. |
| `src/components/Home/index.js` | Serves as the main Home page container component for a React-Redux application. Orchestrates the loading of article feeds (personalized or global based on authentication), popular tags, and dispatches Redux actions for page lifecycle events and tag filtering. | <b>`componentWillMount()`</b>: Determines the appropriate article feed based on user authentication status and dispatches the HOME_PAGE_LOADED action with articles and tags data.<br/><b>`componentWillUnmount()`</b>: Dispatches the HOME_PAGE_UNLOADED action to clean up the component state when the Home page is unmounted.<br/><b>`render()`</b>: Renders the Home page layout including the Banner, MainView for articles, and Tags sidebar with popular tags. |

## `src/constants`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/constants/actionTypes.js` | Defines a centralized registry of Redux action type constants for a React-Redux application implementing the RealWorld example app. Exports string constants representing all state mutation events across authentication, article management, profile interactions, editor operations, and asynchronous flow control. |  |

## `src/reducers`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/reducers/article.js` | Manages the Redux state for a single article page in a RealWorld application. Handles loading and unloading of article data with comments, adding new comments with error handling, and deleting comments by filtering them from the state. | <b>`export default (state = {}, action)`</b>: Redux reducer function that processes article-related actions and returns updated state based on action type, including article page loading/unloading, comment addition, and comment deletion. |
| `src/reducers/articleList.js` | Redux reducer that manages the article list state for the application. Handles actions related to article favoriting/unfavoriting, pagination, tag filtering, tab changes, and loading/unloading of home and profile pages. Maintains state for articles array, article count, current page, active tab, selected tag, and pager configuration. | <b>`export default (state = {}, action)`</b>: Main reducer function that processes dispatched actions and returns updated article list state based on action type. Handles article list updates, pagination, filtering, and page lifecycle events. |
| `src/reducers/auth.js` | Manages authentication state for login and registration flows in a Redux application. Handles state transitions for authentication actions including form field updates, async operation progress tracking, error management, and page cleanup when authentication pages are unmounted. | <b>`export default (state = {}, action)`</b>: Redux reducer function that processes authentication-related actions and returns updated authentication state based on action types including LOGIN, REGISTER, field updates, and page lifecycle events. |
| `src/reducers/common.js` | Implements the Redux reducer for global application state management in the Conduit app. Handles authentication flows (login, register, logout), navigation redirects, user session persistence via token, and page lifecycle events by updating state properties like currentUser, token, redirectTo, and viewChangeCounter. | <b>`export default (state = defaultState, action)`</b>: Main reducer function that processes dispatched actions and returns updated application state based on action type. Manages authentication state, redirects, and view change tracking across the application lifecycle. |
| `src/reducers/editor.js` | Redux reducer that manages the state for the article editor page in a React application. Handles loading and unloading of editor data, article submission with error handling, tag management (adding/removing tags), and dynamic field updates for article properties like title, description, and body. | <b>`export default (state = {}, action)`</b>: Main reducer function that processes editor-related actions and returns updated state. Handles editor page lifecycle, article submission progress and errors, tag list manipulation, and field value updates based on dispatched action types. |
| `src/reducers/home.js` | Manages the Redux state for the home page of a React application. Handles loading and unloading actions by storing tag data when the home page is loaded and resetting state when unloaded. | <b>`export default (state = {}, action)`</b>: Redux reducer function that processes HOME_PAGE_LOADED and HOME_PAGE_UNLOADED actions to manage home page state, specifically handling tag data from API responses. |
| `src/reducers/profile.js` | Redux reducer that manages user profile state in the application. Handles profile page loading, unloading, and user follow/unfollow actions by updating the profile data in the Redux store based on dispatched action types. | <b>`export default (state = {}, action)`</b>: Default exported reducer function that processes profile-related actions and returns updated state. Handles PROFILE_PAGE_LOADED, PROFILE_PAGE_UNLOADED, FOLLOW_USER, and UNFOLLOW_USER action types to manage profile data. |
| `src/reducers/settings.js` | Redux reducer that manages the settings page state for a user profile settings feature. Handles asynchronous save operations by tracking progress status and validation errors returned from the API. | <b>`export default (state = {}, action) => {...}`</b>: Default exported reducer function that processes SETTINGS_SAVED, SETTINGS_PAGE_UNLOADED, and ASYNC_START actions to update the settings state including progress indicators and error messages. |

